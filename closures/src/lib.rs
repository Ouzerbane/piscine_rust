pub fn first_fifty_even_square() -> Vec<i32> {
      (1..=50)
        .map(|x| (x * 2)*(x * 2))
        .collect()
    // let mut i = 0 ;
    // let mut start = 2 ;
    // let mut ve : Vec<i32> = vec![0;50];
    // ve.iter().map
}